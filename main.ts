function circle (text: string, text2: string) {
    aalu = text
    if (aalu == text) {
        game.splash(text)
    } else {
        game.splash(text2)
    }
}
let aalu = ""
circle("abc", "radius")
